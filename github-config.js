// Fill these in once, before publishing the site.
// "owner" is your GitHub username or org, "repo" is the repository name,
// exactly as they appear in the repo's URL: github.com/OWNER/REPO
export const githubConfig = {
  owner: "YOUR-GITHUB-USERNAME",
  repo: "YOUR-REPO-NAME",
  branch: "main",
  // The live GitHub Pages URL for this repo, no trailing slash.
  // e.g. "https://yourusername.github.io/your-repo-name"
  // (or "https://yourusername.github.io" if the repo IS named yourusername.github.io)
  pagesBaseUrl: "https://YOUR-GITHUB-USERNAME.github.io/YOUR-REPO-NAME",
};
